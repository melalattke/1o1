<p id="notice"><%= notice %></p>

<%= image_tag @skin.avatar.url %>
<%= image_tag @skin.avatar.url(:medium) %>
<%= image_tag @skin.avatar.url(:thumb) %>

<p>
  <strong>Name:</strong>
  <%= @skin.name2 %>
</p>

<%= link_to 'Edit', edit_skin_path(@skin) %> |
<%= link_to 'Back', skins_path %>
