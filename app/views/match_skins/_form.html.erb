<%= form_for(@match_skin) do |f| %>
  <% if @match_skin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match_skin.errors.count, "error") %> prohibited this match_skin from being saved:</h2>

      <ul>
      <% @match_skin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Skins_id %><br>
    <%= collection_select(:match_skin,:Skins_id,Skin.all,:id,:name2) %>
  </div>

    <div class="field">
    <%= f.label :Items_id %><br>
    <%= collection_select(:match_skin,:Items_id,Item.all,:id,:name) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
